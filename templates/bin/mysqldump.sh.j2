#!/usr/bin/env bash
docker-compose -f {{ compose_magento_base_directory }}/docker-compose.yml exec -T mysql mysqldump -D {{ compose_magento_database_mysql }} -uroot -p"{{ compose_magento_password_mysql }}"
