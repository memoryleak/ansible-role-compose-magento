#!/usr/bin/env bash
docker-compose -f {{ compose_magento_base_directory }}/docker-compose.yml exec -T \
-w {{ compose_magento_source_directory }} php php -d display_errors=1 -d memory_limit=-1 bin/magento "$@"
